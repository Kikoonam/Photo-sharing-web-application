import styled from "styled-components" 

const Profile =(props)=>{
   return <Container>
    <h2>Profile</h2>
    <h2>Profile</h2>
   </Container>
}

const Container = styled.main`
    position:relative;
`

export default Profile;